---
title: "Running the runtime"
description: "Starting the engine, health, and the surfaces it exposes."
icon: "play"
---

{/* STUB — outline only. Sources: README "Architecture"/CLI, cmd/swarm (serve, health). */}

## swarm serve

{/* TODO: starts runtime, API, health, and MCP surfaces. Ports (8070 healthz). */}

## Boot verification

{/* TODO: a bundle that boots has passed the static analyzer. What boot does (flow tree
  walker, contract merger, boot sequence). Link to /reference/analyzer-checks. */}

## Health

{/* TODO: swarm health / GET /healthz / health.subscribe over WebSocket. */}

## Surfaces

{/* TODO: JSON-RPC API, WebSocket subscriptions, MCP gateway, CLI. Link to API reference. */}
