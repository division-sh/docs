---
title: "Tracing a run"
description: "Following causal traces and reading the flight recorder."
icon: "magnifying-glass-chart"
---

{/* STUB, outline only. Sources: README CLI, FLIGHT-RECORDER.md, cmd/swarm (trace, events,
  conversations, status). */}

## Following a run

{/* TODO: swarm trace [run-id] -f; typed filters; swarm runs; swarm status [run-id]. */}

## The flight recorder

{/* TODO: how to read the event → mutation → agent-turn → delivery timeline. The four tables
  + mutation log. Link to /concepts/persistence-replay-and-fork. */}

## Inspecting events, entities, conversations

{/* TODO: swarm events {list,follow,view}; swarm entities {list,view,aggregate};
  swarm conversations / conversation {view,turn}. */}

## Drift detection

{/* TODO: verify entity_state against the mutation log. */}
