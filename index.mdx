---
title: "Introduction"
description: "Deterministic orchestration for multi-agent LLM systems."
icon: "house"
---

{/* STUB — outline only. Sources: Swarm/README.md, docs/specs/swarm-platform/SWARM-PLATFORM-BRIEF.md */}

Swarm is an event-driven orchestration runtime for multi-agent LLM systems. Flows are
declared as YAML contracts, validated by a static analyzer before boot, and executed by
a deterministic engine. Every interaction is a typed, persisted event — replayable from
the log.

{/* TODO: tighten the one-paragraph pitch. Less an agent library, more a distributed OS for LLMs. */}

## What Swarm is

{/* TODO: 3–4 sentences. Single Go binary + Postgres. Two LLM runtimes today (Anthropic API, Claude CLI subprocess); provider-agnostic factory. */}

## The mental model

{/* TODO: the runtime in six sentences (see platform-guide.md §"The Runtime In Six Sentences"). */}

## Design positions

{/* TODO: pull the opinionated choices from README "Design positions":
  - control loop is deterministic (no LLM routing; CEL conditions)
  - every transition is one transaction
  - flows are composable units (pins)
  - agents are isolated (scoped sessions, hierarchical addressing)
  - execution is replayable
  - runs are forkable
  - humans are a first-class actor (durable mailbox)
  Render as a CardGroup or a definition list. */}

## Where to go next

<CardGroup cols={2}>
  <Card title="Why Swarm" icon="circle-question" href="/why-swarm">
    When this tradeoff is right — and when it is not.
  </Card>
  <Card title="Quickstart" icon="rocket" href="/quickstart">
    Boot the runtime and trigger your first run.
  </Card>
  <Card title="Core concepts" icon="book" href="/concepts/overview">
    Flows, events, handlers, agents, and the engine.
  </Card>
  <Card title="Build a flow" icon="wrench" href="/build/first-flow">
    A support-ticket flow from scratch.
  </Card>
</CardGroup>
