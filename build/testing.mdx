---
title: "Testing and verification"
description: "The static analyzer, test packages, and agent mock fixtures."
icon: "vial"
---

{/* STUB — outline only. Sources: DEVELOPER-GUIDE "Testing Your Flow" + "Checklist Before
  Deploying", README (conformance suite). */}

## The static analyzer

{/* TODO: swarm verify --contracts ./bundle. What it checks; errors abort boot, warnings log.
  Link to /reference/analyzer-checks for the full catalog.
  TODO: reconcile the check count — README says 54, DEVELOPER-GUIDE says 19/16. */}

## Test packages

{/* TODO: a minimal flow + expected.yaml exercising one capability. Worked example. */}

## Agent mock fixtures

{/* TODO: fixtures.yaml — replay fixture events instead of calling the LLM. Test full chains
  without tokens. The conformance suite runs against a scripted LLM. */}

## Pre-deploy checklist

{/* TODO: port the DEVELOPER-GUIDE checklist as a task list. */}
