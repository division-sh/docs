---
title: "Quickstart"
description: "Boot the runtime and trigger your first run."
icon: "rocket"
---

{/* STUB — outline only. Source: README "Quickstart". Keep to ONE happy path. */}

This quickstart boots a Swarm runtime against a contract bundle and triggers a run. It
assumes Docker, a contract bundle, and credentials for one LLM provider.

<Note>
  New to the contract model? Read [Core concepts](/concepts/overview) first, or jump
  straight to [Build a flow](/build/first-flow) to author a bundle from scratch.
</Note>

## Prerequisites

{/* TODO: Docker, a contract bundle path, provider credentials (CLAUDE_CODE_OAUTH_TOKEN or
  Anthropic API key). Link to /installation for the full matrix. */}

<Steps>
  <Step title="Clone and point at a contract bundle">
    {/* TODO: git clone; export SWARM_CONTRACTS_HOST_DIR=/abs/path; add credential to .env */}
  </Step>
  <Step title="Boot">
    {/* TODO: docker compose up -d postgres orchestrator */}
  </Step>
  <Step title="Confirm the runtime is up">
    {/* TODO: curl -fsS http://localhost:8070/healthz — note that a successful boot means
      contracts passed the static analyzer. */}
  </Step>
  <Step title="Trigger a run">
    {/* TODO: docker compose exec orchestrator swarm run --event order.created
      --payload-json '{"order_id":"o-123","priority":"high"}' */}
  </Step>
  <Step title="Follow the trace">
    {/* TODO: swarm trace <run-id> -f */}
  </Step>
</Steps>

## What just happened

{/* TODO: contract bundle → boot verification → engine loaded → event ingested → handler
  pipeline → persisted trace. Point at /concepts/overview. */}

## Next steps

<CardGroup cols={2}>
  <Card title="Build a flow" icon="wrench" href="/build/first-flow">
    Author a contract bundle of your own.
  </Card>
  <Card title="Operating Swarm" icon="gauge" href="/operate/running-the-runtime">
    Trace, fork, and run in production.
  </Card>
</CardGroup>
