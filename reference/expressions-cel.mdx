---
title: "CEL reference"
description: "Context variables and supported expressions for guards, rules, and filters."
icon: "code"
---

{/* STUB, outline only. Sources: platform-guide.md §"Expression Language", DEVELOPER-GUIDE
  "Expressions (CEL)". Concept-level intro lives at /concepts/expressions. */}

## Context variables

{/* TODO: entity, payload, policy, accumulated, fan_out, with the shape of each. */}

## Supported operations

{/* TODO: comparison, membership (in), boolean, arithmetic, .size(), field access.
  TODO: confirm exactly which CEL extensions/functions the engine enables. */}

## Where each context is available

{/* TODO: table, which variables are bound in guard vs rules vs filter vs emit.fields vs
  compute/on_complete. */}

## Prefix requirement

{/* TODO: conditions must be prefixed payload./entity./policy. (dialect). */}
