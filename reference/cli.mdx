---
title: "CLI reference"
description: "Every swarm command and what it targets."
icon: "terminal"
---

{/* STUB, outline only. Sources: README "CLI", cmd/swarm/*.go. Each command targets the
  running orchestrator's v1 RPC unless noted. Consider generating from `swarm --help`. */}

## Runtime

{/* TODO: serve, verify, version, completion <shell>. */}

## Runs

{/* TODO: run, runs, status [run-id], trace [run-id] (-f), health. */}

## Events

{/* TODO: events {list,follow,view}, event {replay,view}, publish <event>. */}

## Entities

{/* TODO: entities {list,view,aggregate}. */}

## Agents and conversations

{/* TODO: agents, agent {view,restart,directive,replay,replay-backlog};
  conversations, conversation {view,turn}. */}

## Control and diagnostics

{/* TODO: incidents, logs, control mailbox {list,view,approve,reject,defer}, control nuke. */}

## Fork

{/* TODO: fork, confirm shipping status / final surface (see /operate/fork-and-replay). */}

<Tip>
  {/* TODO: consider auto-generating this page from `swarm --help` output to prevent drift. */}
</Tip>
