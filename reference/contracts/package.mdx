---
title: "package.yaml"
description: "Flow identity, version, platform constraint, and child flows."
icon: "id-card"
---

{/* STUB — outline only. Source: platform-spec.md §package_yaml, §flow. */}

## Fields

{/* TODO: name, version, description, platform (semver constraint), flows[] (id, flow, mode).
  Use <ParamField> for each. */}

## Child flows

{/* TODO: the flows[] entries; mode: static | template. Link to /build/composition. */}

## Example

{/* TODO: minimal + with-children examples. */}
