---
title: "Subscriptions"
description: "Live traces, event streams, log tails, and health over WebSocket."
icon: "satellite-dish"
---

{/* STUB, outline only. Sources: openrpc.json (run.subscribe_trace, event.subscribe,
  runtime.subscribe_logs, health.subscribe, rpc.unsubscribe). */}

## How subscriptions work

{/* TODO: open a WebSocket, call a subscribe method, receive notifications until
  rpc.unsubscribe. Subscription id semantics. */}

## Available streams

{/* TODO: run.subscribe_trace (live causal trace) · event.subscribe (event log) ·
  runtime.subscribe_logs (log tail) · health.subscribe (health). One example each. */}

## Unsubscribing

{/* TODO: rpc.unsubscribe. */}
